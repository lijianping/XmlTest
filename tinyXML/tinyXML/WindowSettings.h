#ifndef TINYXML_WINDOWSETTINGS_H_
#define TINYXML_WINDOWSETTINGS_H_

#include <string>
#include <map>
using namespace std;

typedef std::map<std::string, std::string> MessageMap;

/* A basic window abstraction - demo purposes only */
class WindowSettings
{
public:
	WindowSettings(void);
	~WindowSettings(void);

};

#endif

